import 'package:flutter/material.dart';

class BlandPage extends StatelessWidget {
  const BlandPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(""),
    );
  }
}
